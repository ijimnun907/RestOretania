{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}


{% block body %}
    <h1>Cambiar la contraseña de {{ user.userIdentifier }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <a class="btn btn-secondary">Volver</a>
    <button type="submit">Guardar</button>
    {{ form_end(form) }}
{% endblock %}
