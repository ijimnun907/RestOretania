{% extends 'base.html.twig' %}

{% block title %}Home Page{% endblock %}

{% block body %}
    <div class="container d-flex flex-column align-items-center justify-content-center" style="min-height: 80vh; text-align: center;">
        <div class="mb-4">
            <img src="{{ asset('images/recursos/logo_tfg.png') }}" alt="Imagen de bienvenida"
                 class="img-fluid rounded shadow" style="max-width: 80%; height: auto;">
        </div>

        {% if not is_granted('ROLE_USER') %}
            <a href="{{ path('app_login') }}" class="btn btn-primary btn-lg">
                Iniciar sesión
            </a>
        {% endif %}
    </div>
{% endblock %}
